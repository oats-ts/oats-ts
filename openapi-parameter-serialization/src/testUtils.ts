import { Options, ParameterValue } from './types'
//                           expected   options      name       value      
export type QueryTestInput = [string, Options<any>, string, ParameterValue, ]
