import { SchemaObjectType } from '@oats-ts/json-schema-model'

export type PrimitiveType = 'boolean' | 'string' | 'number'

export const PrimitiveTypes: SchemaObjectType[] = ['boolean', 'string', 'number', 'integer']
