export type RequestTypesGeneratorConfig = {
  /**
   * If true, cookies will be exposed on the request type.
   * This is mostly useful if you are using this library for testing on node.
   */
  cookies: boolean
}
