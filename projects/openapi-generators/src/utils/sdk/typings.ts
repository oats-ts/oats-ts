export type SdkGeneratorConfig = {
  /**
   * When true, generates documentation for the operations.
   */
  documentation: boolean
  /**
   * When true, cookies can be sent manually from the client side.
   */
  cookies: boolean
}
