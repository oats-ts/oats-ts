export type SdkGeneratorConfig = {
  /**
   * If set to true, the summary and description fields in OperationObjects will be used
   * to generate documentation for the generated methods of the SDK. Otherwise docs will be omitted.
   */
  documentation: boolean
}
