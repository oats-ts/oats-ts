export type ChannelsGeneratorConfig = {
  /**
   * If set to true, the summary, description and deprecated fields will be used
   * to generate documentation for the generated methods. Otherwise docs will be omitted.
   */
  documentation: boolean
}
